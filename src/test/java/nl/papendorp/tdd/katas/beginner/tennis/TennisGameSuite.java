package nl.papendorp.tdd.katas.beginner.tennis;

import org.junit.jupiter.api.DisplayName;

@DisplayName( "Tennis referee" )
class TennisGameSuite
{
	/*
	 * Implement a the scoring of a simple tennis game
	 * *
	 * Rules:
	 * *
	 * Scores from zero up to including three points are described as “love”, “fifteen”, “thirty”, and “forty” respectively.
	 * Ties are described as "all", for example: "fifteen all".
	 * When the score is tied at 3 ("forty all"), this is called deuce. Any further point, gives the scoring player
	 * advantage ("advantage player two").
	 * When an advantaged player scores, he wins the game. When the disadvantaged player scores, however, the score is once
	 * again deuce.
	 *
	 * A game is won by the first player to have won at least four points in total and at least two points more than
	 * the opponent. After the game is won, points can no longer be scored.
	 */
}
