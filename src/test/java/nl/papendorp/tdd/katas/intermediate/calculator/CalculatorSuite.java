package nl.papendorp.tdd.katas.intermediate.calculator;

import org.junit.jupiter.api.DisplayName;

@DisplayName( "A simple calculator" )
class CalculatorSuite
{
	/*
	 * Implement a simple calculator that parses an expression as string and calculates its result as an integer.
	 * It should only accept well-formed strings. Malformed strings should return 0.
	 * Our calculator can add (+), subtract (-), (integer-)divide (/), multiply (*) and remainder (%).
	 *
	 * Examples
	 * "5+5" -> 10
	 * "10-3" -> 7
	 * "4*12" -> 52
	 * "12/4" -> 3
	 * "16/3" -> 5
	 * "5%4" -> 1
	 * "4%5" -> 4
	 * " 5+4" -> 0
	 */
}
